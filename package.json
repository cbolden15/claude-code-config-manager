{
  "name": "claude-code-config-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Personal tool for managing Claude Code project configurations",
  "scripts": {
    "dev": "pnpm --filter server dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "keywords": [
    "claude",
    "claude-code",
    "config-manager"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
